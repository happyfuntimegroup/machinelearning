#!/usr/bin/env python
# coding: utf-8

# In[ ]:


#df['NEW COLUMN NAME'] = df.column.apply(function to be applied)


# In[ ]:


#for ind, row in df.iterrows():
 #   df.loc[ind, "Number of topics"] = len(row['topics'])


# In[ ]:


#for ind, row in df.iterrows():
 #   df.loc[ind, "Number of authors"] = len(row['authors'])    


# In[ ]:


#for ind, row in df.iterrows():
 #   df.loc[ind, "Lenght of the title"] = len(row['title'])  
    
#or 
    
#df['Title Lenght'] = df.title.apply(len)
#df.loc[0:4]

